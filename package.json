{
  "name": "media-suite-site",
  "version": "0.0.1",
  "description": "The Gulp build process for the Media Suite website",
  "scripts": {
    "watch": "cd src && jekyll serve",
    "deploy": "gulp && gulp deploy"
  },
  "private": true,
  "devDependencies": {
    "del": "^2.2.0",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-clean-css": "^3.9.0",
    "gulp-concat": "^2.6.0",
    "gulp-processhtml": "^1.1.0",
    "gulp-rename": "^1.2.2",
    "gulp-rsync": "0.0.8",
    "gulp-uglify": "^1.5.1"
  }
}
